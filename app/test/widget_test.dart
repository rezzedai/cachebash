import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds successfully', (WidgetTester tester) async {
    // Basic smoke test - actual widget tests require Firebase mocking
    expect(1 + 1, 2);
  });
}
